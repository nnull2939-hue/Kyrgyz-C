#include "kyrgyz.h"

#define КЫРГЫЗ_САЛАМ КЫРГЫЗ_СИСТЕМА 

Салам_Кыргызстан
    бүтүн ылдамдык болду
    бүтүн бурч болду
    калкыма убакыт = 0.0 болду
    калкыма g = 9.8 болду
    
    билдирүү("Снаряддын ылдамдыгын жазыңыз (м/с): ") болду
    сура_сан(ылдамдык) болду
    билдирүү("Атуу бурчун жазыңыз (градус): ") болду
    сура_сан(бурч) болду

    калкыма радиан = бурч * (пи / 180.0) болду
    калкыма vx = ылдамдык * косинус(радиан) болду
    калкыма vy_init = ылдамдык * синус(радиан) болду

    кайтала (1) {
        система("clear") болду 
        
        бүтүн x = (бүтүн)(vx * убакыт) болду
        калкыма vy_current = vy_init - g * убакыт болду
        бүтүн y = (бүтүн)(vy_init * убакыт - 0.5 * g * убакыт * убакыт) болду

        билдирүү("===============================================\n") болду
        билдирүү(" БИЙИКТИК: ") болду
        айт_сан(y) болду
        билдирүү(" БАГЫТЫ: ") болду
        эгерде (vy_current > 0.5) билдирүү("ӨЙДӨ ↑\n") болду
        болбосо эгерде (vy_current < -0.5) билдирүү("ТӨМӨН ↓\n") болду
        болбосо билдирүү("ЧОКУСУНДА —\n") болду
        билдирүү("===============================================\n") болду

        эгерде (y < 0 && убакыт > 0.1) {
            үчүн (бүтүн j = 0; j < x; j++) билдирүү(" ") болду
            билдирүү("  💥 БООМ!!!\n") болду
            билдирүү(жаңы_сап) болду
            токтот болду
        }

        бүтүн display_y = (y > 15) ? 15 : y болду
        үчүн (бүтүн i = 0; i < 15 - display_y; i++) {
            билдирүү(жаңы_сап) болду
        }

        үчүн (бүтүн j = 0; j < x; j++) билдирүү(" ") болду
        билдирүү("🚀") болду 
        билдирүү(жаңы_сап) болду

        үчүн (бүтүн i = 0; i < display_y; i++) билдирүү(жаңы_сап) болду
        үчүн (бүтүн j = 0; j < 60; j++) билдирүү("_") болду 
        билдирүү(жаңы_сап) болду

        убакыт += 0.15 болду
        уктоо(80000) болду 
    }

Алга_Кыргызстан